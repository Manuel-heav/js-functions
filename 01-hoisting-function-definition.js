function add(a, b) {
  return a + b;
}

add(2, 2);

// Hositing: Whenever your code runs javascript will do an initial scan of the whole code
// so even if the function was declared before the function definition the snipper will run
